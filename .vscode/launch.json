{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with MSVC",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/g1.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "Build with MSVC"
        }
    ]
}
